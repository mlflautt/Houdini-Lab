# G002-C receipt — deterministic DAG compiler

- Lane: `G002-C`
- Branch: `codex/grinder-g002-c-dag-compiler`
- Base tag: `v0.35.0`
- Base commit: `8bb50a9a78bd870ba0d256fbb52c7aa904d21efa`
- Implementation commit: `abc0fba75c8b9472a388561d146b58bb9b231230`
- Runtime: pure Python 3.11+
- Houdini/license baseline carried as compatibility data: Houdini Apprentice non-commercial,
  `22.0.368`

## Delivered scope

- `hermes_houdini/project_compiler.py`
- `tests/unit/test_project_compiler.py`
- `docs/project-compiler.md`
- this receipt

No sibling lane, schema, execution, graph-batch, export, CLI, fixture, package metadata, workflow,
or root-document path was edited.

## Public API and plan schema

```text
compile_project(spec: Mapping, *, capability_catalog: Mapping,
                adapter_records: Iterable[Mapping]) -> dict
```

The returned schema is `hermes.houdini.project_plan.v1` version `1.0`. It records exact source
hashes and compatibility identity; deterministic project/instance/variant stage IDs; stable
topological order and explicit dependency/contract edges; exact capability and adapter identities;
parent/output/input contracts; graph-edit/cook/cache/render scope; checkpoint boundaries; stage and
aggregate budgets; risk, approvals, evidence, alternatives, and human decision slots; and structured
blockers/warnings. It always sets `automatic_execution: false`, `automatic_ranking: false`, and
`winner: null`.

The canonical `plan_sha256` excludes only its own field. The inline three-variant proof compiled to:

- plan: `a1d9b4bd2623aa90e7ac55b9e99536051e843ee02bbf515ab5f838704dea2807`
- spec: `ab97fdbdfa088edce5a83f7fb4af7a53e4b28f38b235795ebf736fa5973a52da`
- catalog: `b365e308126bdabd53d58211592ff6d4a0de9dcbcad54ce972629dcd5bd1e7ae`
- adapter set: `c518619df3a51299c5f492c4ee2f400d47eeff8079c56fd1d73abbbb5b2df278`

Two fresh Python processes produced the same plan hash. Reversed mapping-key order and reversed
adapter input order also produced byte-equivalent plan mappings. Semantic capability, variant,
evidence, and human-decision list order remains preserved.

## Deliberate blocker coverage

Unit cases cover missing/ambiguous exact capabilities and adapters; disconnected and cyclic graphs;
unresolved duplicate providers; missing dependency stages; undeclared ports and capability output
contracts; capability context/build/license/package drift; adapter context/build/license drift;
unavailable dependencies with denied and explicitly permitted native fallbacks; stage and aggregate
budget overflow including adapter effects; non-finite/programmer type errors; and forbidden
automatic or prefilled creative decisions.

Pending evidence, approvals, human owners/values, three equal-status variants, and null creative
selection fields remain pending/null. Compilation does not promote them to live proof.

## Verification evidence

Startup preflight:

- exact worktree/root/branch/base/tag/remote: pass
- clean checkout at dispatch: pass
- no existing public `compile_project` or `project_plan.v1` conflict: pass
- SSH GitHub authentication: pass (`Hi mlflautt! ... authenticated`)
- GitHub CLI API authentication: token stale at preflight; refresh required before PR creation
- baseline pure suite: `221 passed, 4 skipped`
- baseline Ruff: pass

Implementation verification before the receipt commit:

- `.venv/bin/python -m pytest tests/unit/test_project_compiler.py -o addopts='' -q`:
  `27 passed`
- `.venv/bin/python -m pytest tests/unit -o addopts='' -q` with the cache provider disabled because
  the managed sandbox could not write the external worktree cache: `252 passed`
- `.venv/bin/python -m ruff check .` with `RUFF_CACHE_DIR` under `/private/tmp`: pass
- `git diff --check`: pass
- owned-path audit against the base: pass

Final exact lane commands were then rerun with the prepared worktree writable for normal tool
caches:

- `.venv/bin/python -m pytest tests/unit/test_project_compiler.py -o addopts='' -q`:
  `27 passed`
- `.venv/bin/python -m pytest tests/unit -o addopts='' -q`: `252 passed`
- `.venv/bin/python -m ruff check .`: pass
- `git diff --check`: pass

Evidence statuses for this pure compiler lane:

| Evidence | Status | Reason |
|---|---|---|
| Pure compiler/unit | pass | deterministic and deliberate-failure cases passed |
| Hython import regression | not_applicable | full H22 regression is integration-owned; this module imports without Houdini |
| Graph mutation | not_applicable | compiler cannot mutate a graph |
| Cook/cache/render | not_applicable | scopes are plan metadata only |
| Visual/pixel | not_applicable | no scene or image is produced |
| Plugin/live adapter | not_applicable | adapter presence is metadata, not runtime certification |
| Model/creative quality | not_applicable | no model or aesthetic ranking is used |
| Human decision | not_applicable | decision slots remain null and externally owned |

## Deviations and integration assumptions

There is no functional or ownership deviation from the lane brief. Temporary pytest/Ruff caches
were redirected or disabled for sandboxed verification because the prepared worktree sits outside
the managed writable root; this does not alter tracked files or test semantics.

Lane C imports neither Lane A nor Lane B. Integration must pass their normalized plain mappings.
The exact mapping seam and accepted port/contract forms are documented in
`docs/project-compiler.md`. Package exports, YAML discovery, CLI wiring, execution/resume, and the
Living Biome fixture remain integration-owned. A planned result cannot grant execution; future
execution must revalidate current compatibility, policy, approval, checkpoint, and evidence state.
