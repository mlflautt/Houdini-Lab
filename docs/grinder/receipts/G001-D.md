# Grinder Lane Receipt `G001-D`

- Lane: `G001-D`
- Status: `ready`
- Base tag: `v0.30.0`
- Base commit: `b8b8f4c4b702b4f895bbee3098c90006541a7373`
- Branch: `codex/grinder-g001-d-runner-governance`
- Head commit: this receipt's containing commit; resolve with `git rev-parse HEAD` or the PR head
  (a commit cannot contain its own SHA)
- Pull request: pending at local receipt creation; the final task handoff and PR record are authoritative
- Agent/runtime: `Codex, GPT-5`
- Date: `2026-08-25`

## Delivered contract

Lane D delivers a documentation-only, non-activating design for a future macOS/Houdini runner:

- an asset/boundary/abuse-case threat model covering admission, persistence, credentials, paths,
  caches/artifacts, plugins, general-purpose Houdini/Python execution, egress, resource exhaustion,
  license/concurrency, stale processes, UI interference, audit logs, residual risk, kill switch,
  incident recovery, and a separately owned human activation checkpoint;
- a clean-checkout-to-cleanup local acceptance runbook with frozen Lane A/B/C placeholders,
  explicit tier budgets, plugin modes, bridge/model/human/downstream gates, canonical hashing,
  retention, promotion, cleanup verification, and incident routing;
- a reusable blank release matrix with the five-state vocabulary, required provenance, promotion
  authority, drift invalidation, all required evidence types, and a G001/v0.35 interpretation.

No runner, workflow, secret, setting, license, plugin, model, or Houdini process was activated or
changed. The Hython workflow remains disabled.

## Changed files

- `docs/security/SELF_HOSTED_HOUDINI_RUNNER_THREAT_MODEL.md`
- `docs/acceptance/OPERATIONS.md`
- `docs/acceptance/RELEASE_EVIDENCE_MATRIX.md`
- `docs/grinder/receipts/G001-D.md`

All four are exact Lane D owned paths. No code or `.github/` file changed.

## Verification

| Gate | Status | Exact command | Result or artifact |
|---|---|---|---|
| Startup identity | `pass` | `git rev-parse --show-toplevel`; `git status --short --branch`; `git rev-parse HEAD`; `git remote -v`; `git tag --points-at HEAD` | clean assigned worktree and branch at exact base `b8b8f4c4b702b4f895bbee3098c90006541a7373`; tag `v0.30.0` |
| Authentication preflight | `warn` | `gh auth status --hostname github.com`; `ssh -T git@github.com` | initial restricted preflight: `gh` token reported invalid; SSH DNS failed with `Could not resolve hostname github.com: -65563`; delivery retried separately and final handoff reports outcome |
| Sandboxed pure baseline | `warn` | `.venv/bin/python -m pytest tests/unit -o addopts='' -q` | `168 passed, 4 skipped`; cache write warning explained by restricted worktree permissions |
| Full pure | `pass` | `.venv/bin/python -m pytest tests/unit -o addopts='' -q` | approved native rerun: `172 passed in 4.03s`; the four environment-gated tests passed outside the restricted sandbox |
| Ruff | `pass` | `.venv/bin/python -m ruff check .` | `All checks passed!` on approved native rerun |
| Disabled runner | `pass` | `rg -n "if: false" .github/workflows/ci.yml` | line 41 retains `if: false  # Set to true when runners have Houdini` |
| Relative links | `pass` | `ruby -e 'ARGV.each do ... File.exist?(pth) ... end' docs/security/SELF_HOSTED_HOUDINI_RUNNER_THREAT_MODEL.md docs/acceptance/OPERATIONS.md docs/acceptance/RELEASE_EVIDENCE_MATRIX.md` | no broken repository-relative file links reported |
| Scope | `pass` | `git diff --name-only b8b8f4c4b702b4f895bbee3098c90006541a7373...HEAD` | required final rerun; exact four owned paths only |
| Whitespace | `pass` | `git diff --check` | required final rerun; no output |
| Hython | `not_applicable` | not run | documentation-only lane; no Houdini claim |
| Graph/data/visual | `not_applicable` | not run | documentation-only lane; no graph, cook, render, or aesthetic claim |

## Sourced facts versus proposals

Repository facts are sourced to the frozen workflow, architecture, bridge, resource-control,
Apprentice, plugin-governance, and v0.30 release records. External behavior is sourced only to
official GitHub and SideFX pages with access date `2026-08-25`. The threat model labels host-policy
conclusions as **inference** and all future admission, isolation, egress, retention, ownership,
cleanup, and activation controls as **proposal**. Live GitHub settings were not inspected and are
not claimed.

## Residual risks and activation blockers

- Native Houdini on reused macOS has no demonstrated clean-ephemeral boundary.
- General-purpose code on an admitted protected ref remains powerful.
- Node-locked license concurrency and stale-process recovery need dedicated-host proof and license-owner approval.
- GitHub Actions/fork/branch-protection/runner-group/environment/token settings remain uninspected.
- Current action references are tags, not reviewed full commit SHAs.
- Default-deny egress, external append-only logs, retention/privacy policy, GUI isolation, and host
  reimage/incident drill are not implemented.
- Every activation checklist item is unchecked. A separate human must explicitly approve runner
  activation after implementation evidence; G001 acceptance or merge cannot imply it.

Runner activation therefore remains `blocked` by design.

## Contract and integration notes

- Preserve schema ID `hermes.houdini.acceptance.v1`, exact tier IDs, canonical JSON/hash behavior,
  and the five status values from the accepted manifest.
- Reconcile every `INTEGRATION` placeholder in `docs/acceptance/OPERATIONS.md` against the merged
  Lane A CLI and Lane B/C adapters; use `scripts/run_acceptance.py --help` as the live interface.
- Replace Lane D's `not_applicable` G001 rows with actual integrated evidence statuses. Required
  unrun runtime tiers become `pending` or `blocked`, not documentation-lane `not_applicable`.
- Integration may add shared navigation/release metadata, but must not weaken the threat model's
  separate activation checkpoint or claim runner implementation.
- The integration captain should attach final pure/Hython/live/plugin/visual evidence and hashes;
  Lane D supplies operations and matrix structure only.

## Deviations and unresolved work

- No implementation-scope deviation.
- Initial GitHub authentication/network checks failed as recorded above. Push/PR delivery is a
  required handoff step and will be retried through the approved network path; final delivery state
  must not be inferred from this pre-commit receipt.
- The approved native test environment ran four tests that the restricted sandbox skipped, yielding
  `172 passed` instead of the recorded base expectation `168 passed, 4 skipped`. Both exact outcomes
  are preserved; no test or configuration was changed.
- Hython and visual evidence are intentionally `not_applicable` to Lane D.

## Artifacts and provenance

No generated Houdini, render, cache, model, or other non-Git artifact was produced. This receipt and
the three owned design documents are the complete lane evidence. The final branch head and PR URL
are reported by the task handoff after commit/push/PR creation.
