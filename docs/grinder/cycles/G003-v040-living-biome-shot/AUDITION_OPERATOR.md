# G003-V — Serial Visual Audition Operator

## Mission

Use the three already registered motion capabilities to produce the authentic comparison specified
in `VISUAL_AUDITION.md`. This is the first visible output of G003. It is a bounded live Houdini task,
not a parallel development lane and not permission to continue into Living Biome.

## Start gate

Stop unless all are true:

- `CYCLE_MANIFEST.md` is accepted on protected main and the exact base SHA is recorded;
- the canonical checkout and dedicated audition worktree are clean and derived from that base;
- Houdini is exactly 22.0.368 in Apprentice mode;
- all three capability/recipe identities validate without catalog drift;
- the no-side-effect execution manifest names exact output root, scene paths, seeds, node paths,
  frames, sampled frames, resolution, samples, expected points/primitives, time, memory, bytes,
  checkpoints, rollback, and cancellation points;
- the owner has approved that exact manifest hash and bounds using the Gate V authority; and
- every output path is project-confined and does not exist.

Read `AGENTS.md`, `docs/architecture.md`, `docs/apprentice-constraints.md`, the accepted G003 packet,
the three selected skill manifests/source recipes/docs, `docs/resource-control.md`, and
`docs/verification-ladder.md` before acting. Retrieve official Houdini 22.0.368 documentation and
run read-only operator/parameter probes for every live node type that is not already certified.

## Worktree and outputs

- Worktree: `/Users/m1/houdini-g003-v-audition`
- Branch: `codex/grinder-g003-v-visual-audition`
- Durable artifacts: project-confined ignored `.hermes/g003/gate-v/<run-id>/`
- Repository-owned result: `docs/grinder/receipts/G003-V.md` plus only narrowly required fixes to
  integration-owned audition planning/validation code

Do not commit `.hipnc`, PNG sequences, videos, contact sheets, caches, credentials, raw package
logs, or signing material. The receipt records portable paths, byte sizes, SHA-256 hashes, runtime,
and evidence status.

## Required sequence

1. Re-run the dry manifest in two clean processes and compare canonical hashes.
2. Create new scenes/branches through registered tools and stable Hermes IDs. Checkpoint before
   coherent medium-risk edits. Never rely on selection, active pane, current frame, or desktop.
3. Build Particle Calligraphy, Differential Growth, and native Kinetic Reliquary in stable order.
   Preserve their explicit candidate branches and do not read a human-selected Switch output.
4. Validate graph and sampled data beneath each registered capability ceiling before rendering.
5. Render exactly the approved 12 sampled frames per method with Karma CPU at 640×360 and no more
   than 16 samples. Restore the original frame after every action.
6. Produce playable previews, labeled stable-order contact sheet, graph manifests, mechanics
   reports, and hashes. Retain failed attempts rather than overwriting them.
7. Verify previews actually change over time. A still, duplicated sequence, mock image, or old
   sprint artifact fails Gate V.
8. Present all three without ranking or recommendation. Record human fields as null and stop for
   owner review.

## Stop conditions

Stop on dirty/ambiguous source, runtime/license drift, output collision, missing checkpoint,
unregistered node or code, plugin requirement, blank/cropped/duplicate evidence, budget excess,
cancel request, or any mismatch between approval and execution manifest. Do not silently reduce
quality, replace a method, select a winner, or proceed into lane launch.

## Receipt and handoff

The receipt includes base/head, manifest and approval hashes, exact build/license/package/catalog,
commands, stage table, graph/data/render metrics, scene/artifact hashes, failed attempts, frame
restoration, warnings, and pending human decision. Commit the receipt and any authorized narrow
code only, push, open an unmerged PR, and give the orchestrator the three artifact paths/hashes for
owner review.
