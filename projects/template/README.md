# Project: template

Copy this folder to start a new Houdini creative project. Keep generated `.hipnc`/`.hdanc`
out of Git (gitignored). Track `project.toml`, manifests, recipes, and source scripts.

Set `$JOB` to this folder root when working in Houdini.
