// template: growth_mask
// attributes in:  v@P
// attributes out: float growth
// variables: bias (float 0..1), radius (float)
// radial growth bias field centered at origin.
float bias = 0.65;
float radius = 2.0;
float d = length(@P) / radius;
@growth = clamp(1.0 - d + (bias - 0.5), 0.0, 1.0);
